// Page-specific validations or overrides for user form
(function(window){
  function init(){
    // placeholder for page-specific rules; currently none
  }
  $(function(){ init(); });
  window.OamuserValidate = { init: init };
})(window);
